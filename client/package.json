{
  "name": "@localbird/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node -e \"console.error('\\nDo not deploy client/ as its own Railway service.\\nDeploy the repo root instead — npm run build && npm start serves client/dist from the server.\\n'); process.exit(1)\""
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.6.2"
  },
  "devDependencies": {
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "vite": "^7.0.0"
  }
}
